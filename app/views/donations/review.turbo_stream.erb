<turbo-stream action="update" target="donation_form">
  <template>
    <%= render partial: "donations/review", locals: { @amount, @contact_email, @contact_first_name, @contact_last_name, @amount_in_cents } %>
  </template>
</turbo-stream>